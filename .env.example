# Flaming Horse Environment Configuration
# Copy this file to .env and fill in your API keys

# ============================================
# LLM Provider Configuration (provider-agnostic)
# ============================================
# Uncomment ONE provider below:
LLM_PROVIDER=XAI
# LLM_PROVIDER=MINIMAX

# ============================================
# Provider-Specific Keys (all defined, one used based on LLM_PROVIDER)
# ============================================
XAI_API_KEY=your_xai_key_here
MINIMAX_API_KEY=your_minimax_key_here

# ============================================
# Provider-Specific Base URLs (defaults provided if omitted)
# ============================================
XAI_BASE_URL=https://api.x.ai/v1
MINIMAX_BASE_URL=https://api.minimax.io/v1

# ============================================
# Provider-Specific Models (AGENT_MODEL used as fallback if omitted)
# ============================================
XAI_MODEL=grok-code-fast-1
MINIMAX_MODEL=MiniMax-M2.5

# ============================================
# Global Fallback Model (used if provider-specific model not set)
# ============================================
# AGENT_MODEL=grok-code-fast-1

# ============================================
# Other config (unchanged)
# ============================================
USE_HARNESS=1

# Voice TTS backend
FLAMING_HORSE_TTS_BACKEND=mlx
FLAMING_HORSE_MLX_PYTHON=/Users/velocityworks/IdeaProjects/flaming-horse/models/qwen3-tts-local/mlx_env312/bin/python
FLAMING_HORSE_MLX_MODEL_ID=mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit

# Voice reference directory (optional - overrides voice_clone_config.json)
# Set to switch between reference samples easily, e.g.:
#   FLAMING_HORSE_VOICE_REF_DIR=/Users/velocityworks/IdeaProjects/flaming-horse/assets/voice_ref
#   FLAMING_HORSE_VOICE_REF_DIR=/Users/velocityworks/IdeaProjects/flaming-horse/assets/voice_ref_2

# Offline mode for HuggingFace
HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1
TOKENIZERS_PARALLELISM=false

# Parallel rendering (0=auto, N=use N jobs, -1=disable)
PARALLEL_RENDERS=0

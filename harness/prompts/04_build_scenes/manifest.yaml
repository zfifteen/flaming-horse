phase: build_scenes
status: active
tasks:
  - name: default
    system: system.md
    user: user.md
required_placeholders:
  system: [core_rules, build_scenes_system, template_doc, config_guide, visual_helpers]
  user: [scene_id, scene_file_name, scene_class_name, narration_key, scene_title, scene_details, scene_narration, reference_section, retry_section]

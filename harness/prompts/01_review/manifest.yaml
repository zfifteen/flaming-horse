phase: review

# This phase is currently a stub and is not LLM-driven in the harness.
# Structural validation is performed deterministically by the orchestrator before advancing to narration.

inputs:
  - plan_json: project_dir/plan.json
  - constraints: required_fields_present, scene_count(12-24)

output:
  format: n/a
  file: n/a
  required_fields: []

validation:
  requires_plan_json: true
  fails_if_missing_required_scene_fields: true
